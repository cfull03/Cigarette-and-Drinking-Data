# environment.yml (cleaner & safer)
name: cigarette-and-drinking-data
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - ipykernel
  - jupyterlab
  - numpy>=1.26
  - pandas>=2.2,<3
  - matplotlib
  - pyarrow
  - scikit-learn
  - typer
  - loguru
  - tqdm
  - ruff
  - pytest
  - mkdocs
  - python-dotenv
  - xgboost
  # ⬇️ remove the editable install from here to avoid PATH/pip mismatch during solve
  # - pip:
  #     - -e .
